'use strict';

/**
 * enable-buy-design service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::enable-buy-design.enable-buy-design');
